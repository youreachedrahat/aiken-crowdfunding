import { Cloneable } from "../utils/Cloneable.js";
import { ToRawObj } from "./interfaces/ToRawObj.js";
import { ICborObj } from "./interfaces/ICborObj.js";
export type RawCborText = {
    text: string;
};
export declare function isRawCborText(t: RawCborText): boolean;
export declare class CborText implements ToRawObj, Cloneable<CborText>, ICborObj {
    get text(): string;
    chunks: string | CborText[];
    get isDefiniteLength(): boolean;
    addInfos: number;
    constructor(text: string | CborText[], addInfos?: number);
    toRawObj(): RawCborText;
    clone(): CborText;
}
